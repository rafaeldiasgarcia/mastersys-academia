services:
  postgres:
    image: postgres:16
    container_name: postgres_academia
    environment:
      POSTGRES_DB: academia
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_academia:/var/lib/postgresql/data

volumes:
  postgres_data_academia: